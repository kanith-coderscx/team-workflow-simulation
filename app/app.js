// Boot for the expense/income tracker.
// The initial shell only renders static placeholders; each feature (issues #1-#3)
// replaces its section's placeholder with working UI on its own branch.

document.addEventListener('DOMContentLoaded', () => {
  console.log('[app] booted — awaiting feature implementations');
});
